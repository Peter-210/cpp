services:
  cpp-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cpp-container
    volumes:
      - .:/app
    entrypoint: /bin/bash
    stdin_open: true
    tty: true
    security_opt:
      - seccomp=unconfined
